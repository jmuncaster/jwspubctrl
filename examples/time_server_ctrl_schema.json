{
    "oneOf": [
        { "$ref": "#/definitions/format_request" }
    ],
    "definitions": {
        "format_request": {
            "title": "Time Server Format Request",
            "type": "object",
            "properties": {
                "format": { "type": "string" }
            },
            "required": ["format"]
        }
    }
}
