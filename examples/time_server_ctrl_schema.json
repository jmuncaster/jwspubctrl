{
    "oneOf": [
        { "$ref": "#/definitions/format_request" },
        { "$ref": "#/definitions/reply" }
    ],
    "definitions": {
        "format_request": {
            "title": "Time Server Format Request",
            "type": "object",
            "properties": {
                "format": { "type": "string" }
            },
            "required": ["format"]
        },
        "reply": {
            "title": "Time Server Reply",
            "type": "object",
            "properties": {
                "error": { "type": "boolean" },
                "message": { "type": "string" }
            },
            "required": ["error", "message"]
        }
    }
}
